# CBT・医師国家試験 医学学習ノート作成ルール

## 目的

このフォルダは、CBT と医師国家試験に向けて、医学用語と過去問を関連づけながら整理するための学習ノート集である。

用語を単独で暗記するのではなく、病態・症候・検査・治療や過去問とのつながりが短時間で把握できるノートを作る。卒前教育と医師国家試験の範囲を基本とし、過度に専門的な内容や臨床現場向けの細かな例外は避ける。

## 基本方針

- ユーザーから提示された医学用語について、作成時点で信頼できる情報源を検索して確認する。
- 医学的な正確性を優先し、不確かな内容を推測で補わない。
- CBT・医師国家試験で問われやすい定義、原因、病態、主要症候、検査、治療、関連語を中心にする。
- 一読して全体像をつかめる、短く平易な日本語で書く。
- **情報量は最小限にする。**原則として「定義 + 重要ポイント3～5個 + CBTチェック1～3個」で完結させる。
- 代表例は最大5個までとし、網羅的な列挙、詳細な手順、頻度の低い例外は省略する。
- 複数の内容を比較する必要があるときだけ、5行程度までの小さな表を使う。
- 詳細な説明が必要になっても、まずは要点を残す。追加情報はユーザーから求められた場合に限る。
- 略語は初出時に正式名称を併記する。
- 数値や基準値は学習上重要なものだけを載せ、施設差やガイドライン差がある場合はその旨を短く記す。
- 鑑別や緊急性に直結する重要事項は省略しない。
- 患者個人への診断・治療指示ではなく、医学教育用の説明として記述する。

## 調査と情報源

- 原則としてインターネット検索を行い、複数の情報を照合する。
- 公的機関、学会、診療ガイドライン、標準的な教科書に相当する信頼性の高い資料を優先する。
- 医療機関の一般向け解説や査読論文は、補助的に用いる。
- 出典不明のまとめサイト、広告目的のページ、生成 AI の回答のみを根拠にしない。
- 最新情報が CBT の標準的理解と異なる場合は、CBT で必要な基本事項を本文に置き、必要なら注記する。
- 調査に用いた資料をノート末尾へ記載する必要はない。
- 原文の長い引用は避け、自分の言葉で要約する。

## フォルダ分類

用語ノートは内容に最も適した次のフォルダへ保存する。必要なフォルダは作成してよい。

- `01_基礎医学/`：解剖、生理、生化学、免疫、微生物、病理、薬理など
- `02_症候・診察/`：症候、身体診察、医療面接、鑑別の入口となる概念
- `03_疾患/`：個別の疾患・病態。必要に応じて循環器、呼吸器など診療科別の下位フォルダを作る
- `04_検査/`：検体検査、生理検査、画像検査、検査所見など
- `05_治療/`：治療法、処置、手術、輸液、リハビリテーションなど
- `06_薬剤/`：薬剤、薬効分類、作用機序、副作用など
- `07_公衆衛生・法医学/`：疫学、予防、医療制度、医療倫理、法医学など

複数分野にまたがる用語は、主題となるフォルダに1つだけ配置し、関連ノートからリンクする。分類が不明確なら、無理に深い階層を作らず最も近い上位フォルダへ置く。

### 医師国家試験の過去問

- 過去問ノートは7分類の例外として、vault直下の `医師国家試験/` に保存する。
- `医師国家試験/<回数>/<ブロック>/<番号>.md` の階層で保存する（例：`医師国家試験/95回/I/20.md`）。
- 問題文と選択肢を正確に転記し、正答、簡潔な根拠、必要最小限の誤答ポイントを記載する。
- 解説中の疾患・検査・治療は既存の用語ノートへリンクし、リンク先が未作成なら `[[用語名]]` を残す。
- 問題文だけのスクリーンショットは保存せず、画像診断や病理像など設問理解に必要な画像のみ `_attachments/` に保存する。
- 医師国家試験の設問に画像診断・内視鏡・病理・グラフなどの設問理解に必要な画像がある場合は、`医師国家試験/<回数>/<ブロック>/_attachments/` に保存し、対応する問題ノートの問題文中へ直接埋め込む。
- 国試画像を用語ノートへ追加する場合でも、用語ノートだけへの掲載で済ませず、元の過去問ノートにも必ず直接埋め込む。過去問ノートへの画像追加を完了条件とする。
- 国試画像の直前または直後に主要所見を1行で示し、出典、問題番号、個人学習用である旨を画像の近くへ記載する。

### CBT短答模擬問題

- 短答模擬問題は7分類の例外として、vault直下の `CBT短答模擬問題/` にまとめる。医学用語の分類フォルダへ混在させない。
- ファイル名は `第<回数>回.md` とする（例：`CBT短答模擬問題/第1回.md`）。新規作成前に既存回を検索し、次の連番を使う。
- `CBT短答模擬問題/README.md` を索引とし、各回へのリンク、作成日、状態（未回答・回答済み・採点済み）、得点を記録する。
- 作成時は、問題ごとに `**あなたの回答：**` 欄を設ける。正答は回答前に同じファイルへ表示せず、採点時に追記する。
- CBT短答模擬問題では、問題文、回答欄、ユーザーの回答にMarkdownリンクやObsidianリンクを付けない。リンクによって正答の手掛かりを与えないため、医学用語のリンク化は採点後の正答・解説部分だけで行う。
- 正答・解説に記載する医学用語は、既存ノートがある場合はMarkdownリンク、未作成の場合は `[[用語名]]` とする。採点結果の得点・誤りの種類など、医学用語でない語句はリンク化しない。
- 第2回以降の新規セットは、既出問題の単純な反復ではなく未発見の弱点を探すことを主目的とする。原則として全体の70〜80%を未出の領域・論点から出題し、残りは過去の弱点を聞き方や症例、問う深さを変えて確認する。
- 既出問題そのものの定着は、採点済みファイルの再挑戦機能で行う。新規セットでは診療科と「病態・症候・検査・治療」の問い方が偏らないようにする。
- 採点は、原則として設問が要求する各回答要素を1点とし、不十分だが核心に近い回答には0.5点を認める。採点基準、得点、正答、最小限の修正点、誤りの種類を記録する。
- 採点後も問題文とユーザーの回答を改変しない。問題に不備があった場合は、その設問を採点対象外として理由を明記する。
- ユーザーが再挑戦する場合は、HTMLの `<details>` 折りたたみを使わない。採点済みの問題・回答・解説を `CBT短答模擬問題/採点履歴/第<回数>回_第<試行回数>回.md` へ移し、元の `第<回数>回.md` は問題文と空の `**今回の回答：**` 欄だけにする。
- 再挑戦用ファイルから採点履歴や正答へリンクしない。採点履歴へのリンクは `README.md` の索引だけに置き、解答中に正答が見えない状態を保つ。
- 再採点後も試行履歴を消さず、試行ごとに採点履歴ファイルを分ける。索引の状態は「再挑戦中」または「再採点済み」とする。
- 作成・移動・採点の完了時は、ユーザーがすぐ開けるよう対象ファイルのパスを報告する。

### フォルダ運用

- 用語ノートは上記7分類のいずれかへ置き、ルート直下や独自の分類フォルダへ置かない。過去問ノートは `医師国家試験/`、短答模擬問題は `CBT短答模擬問題/` に置く。
- 診療科・検査種別・薬効群など、同じ分野のノートが増えた場合は2階層目のフォルダを作って整理する。
- 分野横断のまとめ（MOC）も、独立した `MOC/` ではなく主題に対応する分類フォルダへ置く。
- `.obsidian/`、`copilot/`、`Templates/` はアプリ・プラグイン管理用のため、医学ノートの分類対象外とする。
- 空ファイル、空フォルダ、用途不明の一時ファイルを残さない。

## ファイル名とリンク

- 1用語につき原則1ファイルとする。
- ファイル名は `用語名.md` とし、一般的な日本語表記を使う。
- ファイル名に使用できない記号は全角化または省略する。
- 同義語や英語名だけが異なる場合は重複ファイルを作らず、本文冒頭に併記する。
- 既存ノートと関連する場合は、Markdown リンク `[表示名](相対パス)` で相互に結びつける。
- 強調したい医学用語は太字 `**用語**` ではなく、Obsidian リンク `[[用語]]` で記載する。
- `[[用語]]` はリンク先がまだ存在しない場合もそのまま残すが、空ファイルは作らない。
- 文章上の単なる強調には太字を使用してよいが、ノート化できる医学用語は `[[用語]]` を優先する。

## 画像の運用

- ユーザーが画像を提示した場合は、内容を確認し、病態・画像所見・解剖・計算の理解に直接役立つものだけをノートへ追加する。
- 画像の追加は、図・画像所見によって文章だけより理解や鑑別が明確になる場合に限る。単なる装飾、問題文の再掲、短い文章で十分に伝わる内容には追加しない。
- 問題文や選択肢だけを写したスクリーンショットは原則として保存せず、図表・画像検査・模式図など学習価値のある画像を優先する。
- インターネットから画像を追加する場合は、医学的内容を信頼できる情報源で確認し、利用条件を確認する。公的機関・学会・大学の公開資料、パブリックドメイン、または再利用可能なライセンス（例：Creative Commons）の画像を優先する。
- Creative Commonsなどで表示・出典の明記が必要な画像は、画像の近くに著者・出典ページ・ライセンスを記載する。
- インターネット上から新たに取得する著作権・利用条件不明の画像は、ローカル保存して使用しない。必要なら公開資料へのリンクまたは自作の簡潔な模式図を検討する。
- ユーザーが自ら提示し、この私的vaultへの保存を明示的に希望した画像は、個人学習用資料として保存してよい。出典不明の画像は公開・再配布可能な素材として扱わない。
- 採用した画像は、ノートと同じフォルダ内の `_attachments/` に分かりやすい日本語ファイル名で保存し、Markdown形式 `![簡潔な説明](_attachments/画像名.png)` で該当ノートに埋め込む。
- 医師国家試験の設問画像では、対応する過去問ノートを画像の第一の掲載先とする。関連する用語ノートへの掲載は追加対応であり、過去問ノートへの直接埋め込みの代わりにはならない。
- 一時フォルダのパスや外部URLを画像の保存先として使わない。ノートを移動しても参照できるローカル画像を使用する。
- 画像の直前または直後に、何を示す画像かを1行で分かるようにする。画像だけで診断を確定するような誤解を招く場合は、本文に限界や確定に必要な検査を併記する。
- 既存ノートに関連する画像を追加したときは、画像の内容と本文・リンクに矛盾がないか確認する。

## 標準テンプレート

用語の性質に応じて不要な節は省略し、必要な節へ置き換えてよい。**見出しは原則2～3個まで**とし、空の見出しは残さない。

```markdown
# 用語名

> 1～2文で定義と最重要ポイントを示す。

## 要点

- 最重要事項
- 代表的な特徴・所見・薬剤など
- 間違えやすい対比

## CBTチェック

- 問われやすい対比、組合せ、典型像を1～3項目で確認する

```

医師国家試験の過去問には次の簡潔な形式を用いる。

```markdown
# 回数 ブロック-番号

## 問題

問題文

- a. 選択肢

## 正答・解説

**正答：記号 選択肢**

- 正答の根拠
- 必要な場合のみ誤答ポイント
```

CBT短答模擬問題は、問題・回答欄に続けて採点結果を追記する。

```markdown
# CBT短答模擬問題 第N回

## 問題

**問1**

問題文

**あなたの回答：**

## 採点結果

> 得点：x / y点（z%）

- 問1（x / y点）正答：〇〇。修正点：〇〇。

## 総評

- 定着している点
- 優先して復習する点
```

## 表現上のルール

- 見出しと箇条書きを活用し、長い段落を避ける。
- 本文の説明は原則として1項目1行にする。
- 強調は本当に重要な語句に限り、太字を多用しない。
- 専門用語には、必要に応じて短い言い換えを添える。
- 病態の流れは `原因 → 変化 → 症候` のように矢印で簡潔に示してよい。
- 表は比較が有効な場合だけ使い、最大3列・5行程度にする。
- 語呂合わせは、正確で誤解を招かない場合に限って補助的に使う。
- 「必ず」「絶対」など例外を無視する表現を避ける。

## 作成前後の確認

1. 既存ファイルを検索し、重複や関連ノートを確認する。
2. 信頼できる情報源を検索し、内容を照合する。
3. 適切なフォルダとファイル名を決める。
4. CBT・医師国家試験の学習範囲に絞ってノートを作る。
5. 関連ノートへのリンクを追加し、必要なら既存ノート側にもリンクを追加する。
6. 誤字、リンク切れ、空見出し、過度に専門的な記述がないか確認する。

## M3E CBT連携ルール

### 演習画面を開く

- ユーザーがプロンプトで `go` と送ったら、Codexのブラウザで `https://m3e-medical.com/users/cbt` を通常のブラウザタブとして開く。
- `go` の操作では、ピクチャ・イン・ピクチャ表示を開始・表示しない。ブラウザの表示制御で右下のフローティング小窓が出る操作は行わない。

### `done` の実行フローと取得完了通知

- ユーザーがM3Eの結果画面で `done` と送ったら、**結果取得だけで終了せず**、原則として同じターン内で「結果取得 → 取得完了通知 → 誤答分析 → 既存ノート検索 → 情報源照合 → ノート作成・更新 → リンク確認 → 最終報告」まで自動で行う。
- 医学情報の検索やノート編集より先に、その時点の結果画面全体を取得する。
- M3Eに表示される「前回の回答」は、ユーザーがその設問で直前に選んだ選択肢として扱う。
- 取得開始時に、URL内の演習IDと画面内の問題番号を組み合わせた取得IDを定める。
- 画像問題では、結果画面の文字情報と同じ取得段階で設問に対応する臨床画像・検査画像も一時取得する。
- 必要な文字情報と画像を取得して整合性を確認したら、`取得完了：問題番号（取得ID）` を**中間の進捗通知**として返す。これは作業終了の最終回答ではなく、通知後も調査・分析・ノート編集を続ける。ユーザーはこの通知後に次の問題へ進める。
- `done` に対する最終回答は、ノート作成・更新と確認が完了した後に、誤答分析の要点および作成・更新したファイルのパスを報告して行う。
- 関連する既存ノートがなくても、CBT・医師国家試験で再利用できる論点がある場合は、適切な分類フォルダへ新規ノートを作成する。既存ノートで十分な場合も、今回の設問から得た重要事項が欠けていれば追記する。
- 取得した問題が医師国家試験で画像を含む場合は、画像の採否を決めたうえで、採用画像を過去問ノートへ直接埋め込んでから最終報告する。用語ノートだけに画像を追加した状態では完了としない。
- Codex側から「次の問題」へ移動する操作は行わない。
- ユーザーが明示的に指示するまで、モントレの問題画面タブを閉じない。取得・調査・ノート作成の完了後も開いたまま維持する。

### 画像の一時取得と採否

- 結果画面に臨床画像・検査画像・病理像・模式図など設問理解に必要な画像がある場合は、スクリーンショットではなく可能な限り画像ファイル本体を取得する。
- 取得した画像は、取得IDごとに分離した一時保存先へ置く。ノートへの採否が決まるまではvault内の `_attachments/` へ入れない。
- 連問では画像を「取得ID・問題番号・画像ファイル」の組で管理し、どの設問に対応するか確認できない画像は使用しない。
- 各画像の状態を取得ID内で「一時保存中・採用済み・破棄済み」のいずれかとして管理し、未処理の画像を別の取得IDへ持ち越さない。
- 医学的要点、画像所見、本文との対応を検討し、文章だけより理解や鑑別が明確になる画像だけを採用する。
- 採用する画像は、関連ノートと同じフォルダの `_attachments/` へ分かりやすい日本語名で保存し、画像の意味を示す1行とともにMarkdownで埋め込む。M3Eの問題番号と、個人学習用である旨を画像の近くに記載する。
- 採用しない画像は、その取得IDの処理完了時に一時保存先から破棄する。採否未決定の画像、一時フォルダ、重複画像をvault内に残さない。
- 新しい `done` を受け取った場合は、新しい取得ID専用の一時保存先を作り、以前の取得IDの画像を流用しない。

### 問題混同の防止

- 取得開始時と終了時にURLを確認し、同一URLかつ結果画面（`#result`）であることを照合する。途中でURLが変わった場合、その取得は破棄して再取得する。
- 連問では各設問の「問題番号・問題文・選択肢・前回の回答・正答・正誤・解説」をひとまとまりとして取得し、表示順だけを根拠に対応づけない。
- 設問数、問題番号数、結果欄数、解説番号数が一致しない場合は処理を止め、再取得またはユーザー確認を行う。欠けた内容を推測で補わない。
- 画像問題では、取得完了を返す前に設問と画像の対応、および取得IDごとの一時保存を確認する。最終的な採否と保存方法は「画像の運用」と「画像の一時取得と採否」に従う。
- 取得後の調査・ノート作成には取得済みの内容を使い、ユーザーが移動した後のライブ画面を前の問題の根拠として再利用しない。
- 新しい `done` は必ず別の取得IDとして扱い、前の問題の問題文・回答・解説・画像・作成ファイルと混ぜない。
- 各取得IDの状態を「取得済み → 調査中 → ノート作成済み → 確認済み」と管理し、完了していない処理を明確にする。
- ノート作成中に新しい `done` を受け取った場合は、現在の編集を安全な区切りで止め、新しい結果画面を別の取得IDとして先に取得・通知してから、未完了の作業へ戻る。
- 結果画面の情報が欠ける、矛盾する、または設問との対応を確定できない場合は、推測せずユーザーに該当結果画面の再表示を依頼する。

### 分析とノート作成

- 誤答分析では「前回の回答」と「正答」の差を明示し、知識不足・読み違い・鑑別不足など、誤りの種類を簡潔に分類する。
- M3Eの問題文や解説を学習ノートへ長く転記せず、信頼できる資料で照合した医学的要点を自分の言葉で要約する。
- 既存ノート検索、情報源照合、ファイル作成、リンク確認の順に行い、完了時に作成・更新したファイルを報告する。
